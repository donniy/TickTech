cache:
    paths:
        - app/frontend/node_modules
        - venv
        - app/frontend/package-lock.json
install:
    stage: build
    script:
        - ./INSTALL
        - make install
build:
    stage: build
    script:
        - make build
unit test:
    stage: test
    script:
        - make test
